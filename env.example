# 环境变量配置示例
# 复制此文件为 .env.local 并根据需要修改

# 强制使用 Vercel KV 存储（即使在本地开发）
# 设置为 true 时，本地开发将连接远程 KV 而不是使用文件系统
# USE_KV_STORAGE=true

# Vercel KV 配置（使用 KV 时必需）
# 从 Vercel 控制台或 `vercel env pull` 获取这些值
# KV_REST_API_URL=https://your-kv-url.vercel.app
# KV_REST_API_TOKEN=your-kv-token

# API 基础 URL（可选）
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000